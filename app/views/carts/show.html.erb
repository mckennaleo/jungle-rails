<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <% if cart.empty? %>
    <%= render "cart_empty" %>
  <% else %>
    <%= render "cart_full" %>
  <% end %>

</section>